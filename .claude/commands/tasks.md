---
description: List all tasks from your ClickUp list (176135389)
allowed-tools: mcp__clickup-taskmaster__list_tasks
---

Use the `list_tasks` tool from the clickup-taskmaster MCP server to retrieve all tasks.

Show the results in a clean, organized format:
- Group by status if possible
- Show task name, status, priority, and due date
- Highlight any urgent or overdue tasks

If the user provided arguments: $ARGUMENTS

Use those to filter (e.g., status, assignee, order).
